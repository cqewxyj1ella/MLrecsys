# MLrecys

A repo for Machine Learning course's project: applications on Recommendation System.

`lgb_main.py` is a source code we get from CSDN. It simply called `LGBClassifier` from `lightgbm`, and got score of $0.207$.
